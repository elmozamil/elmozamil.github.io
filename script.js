const toggle=document.querySelector('.menu-toggle');const links=document.querySelector('.nav-links');const year=document.querySelector('#year');if(year)year.textContent=new Date().getFullYear();if(toggle&&links){toggle.addEventListener('click',()=>{links.classList.toggle('open')});links.querySelectorAll('a').forEach((link)=>links.classList.remove('open'))}const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')})},{threshold:.12});document.querySelectorAll('section > .container, .card, .value-card, .journey-card, .featured-project-card, .future-projects').forEach(el=>{el.classList.add('reveal');observer.observe(el)});
